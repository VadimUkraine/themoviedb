{
  "name": "MoviesFS",
  "version": "1.0.0",
  "description": "Full Stack App with NodeJS, Apollo Client, React, TypeScript and GraphQL",
  "private": true,
  "author": "VO",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"yarn --cwd client dev\" \"yarn --cwd server dev-server\"",
    "install-modules": "concurrently \"yarn --cwd client install-modules\" \"yarn --cwd server install-modules\" && yarn start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
